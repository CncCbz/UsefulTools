# 🛠️ UsefulTools

> 开发者实用工具集 — 基于 Tauri 2 + Vue 3 的 Windows 桌面应用

一个集合了常用开发/设计小工具的桌面应用，所有工具通过插件系统按需安装，开箱即用，界面采用「块状工作室」风格设计。

## ✨ 特性

- 🔌 纯插件化架构 — 主应用不内置任何工具，所有工具通过插件商店从 npm 安装
- 🧰 官方插件包提供 27 款工具，覆盖编码开发、文本处理、转换解析、生成工具、设计工具五大分类
- 🔍 全局搜索（Ctrl+K）+ 分类筛选 + 命令面板，快速定位工具
- ⭐ 收藏常用工具，支持拖拽排序自定义布局
- 📦 设置/收藏/排序数据导入导出
- 🐛 调试模式 — 从本地目录加载未发布的插件，开发时实时预览
- 🎨 深色主题 + 金黄主色调，硬阴影卡片风格
- ⚡ 基于 Tauri 2，体积小、启动快、资源占用低

## 🧰 官方工具列表

| 分类 | 工具 |
|------|------|
| 编码开发 | JSON 格式化 · JS 代码运行器 · JSON 对比 · 正则测试 · 加解密工坊 · Hash 生成器 · URL 编解码 · HTML 实体编解码 · SQL 格式化 |
| 文本处理 | Google 翻译 · 文本对比 · Markdown 预览 · 大小写转换 · 文本统计 |
| 转换解析 | 时间戳转换 · Cron 表达式 · JWT 解析器 · 进制转换 · JSON ↔ YAML · IP/子网计算器 · Chmod 计算器 |
| 生成工具 | UUID 生成器 · Lorem Ipsum · QR 码生成器 · 密码生成器 |
| 设计工具 | 颜色转换器 · 图片压缩转换 |

## 🏗️ 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 桌面框架 | [Tauri](https://tauri.app/) | 2.x |
| 前端框架 | Vue（Composition API + `<script setup>`） | 3.5+ |
| 语言 | TypeScript + Rust | 5.6 / 1.89+ |
| 样式 | Tailwind CSS（`@tailwindcss/vite`） | 4.x |
| 构建 | Vite | 6.x |
| 路由 | vue-router（Hash 模式） | 4 |
| 包管理 | pnpm | 10+ |
| 字体 | Space Grotesk（本地） | - |
| 图标 | Material Icons（本地） | - |

## 🚀 快速开始

### 环境要求

- [Node.js](https://nodejs.org/) >= 18
- [pnpm](https://pnpm.io/) >= 8
- [Rust](https://www.rust-lang.org/tools/install) >= 1.77
- Tauri 2 系统依赖（参考 [Tauri 官方文档](https://tauri.app/start/prerequisites/)）

### 安装与运行

```bash
# 克隆仓库
git clone https://github.com/your-username/usefultools.git
cd usefultools

# 安装依赖
pnpm install

# 启动 Tauri 桌面应用（开发模式，首次编译约 2-5 分钟）
pnpm tauri dev

# 仅启动前端开发服务器（端口 1420，无 Tauri 能力）
pnpm dev
```

### 构建生产版本

```bash
pnpm tauri build
```

构建产物位于 `src-tauri/target/release/bundle/`，Windows 下生成 NSIS 安装包。

## 📁 项目结构

```
UsefulTools/                         # 主应用
├── src/
│   ├── main.ts                      # Vue 应用入口
│   ├── App.vue                      # 根组件（首页/商店/工具页三种布局）
│   ├── styles/main.css              # 全局样式 + Tailwind @theme 配置
│   ├── components/
│   │   ├── TitleBar.vue             # 自定义标题栏
│   │   ├── Sidebar.vue              # 首页左侧导航
│   │   ├── DashboardHeader.vue      # 首页顶部搜索栏
│   │   ├── CategoryTabs.vue         # 分类标签栏
│   │   ├── ToolCard.vue             # 工具卡片
│   │   ├── PluginCard.vue           # 插件商店卡片
│   │   ├── PluginDetailModal.vue    # 插件详情弹窗
│   │   ├── ToolSidebar.vue          # 工具页左侧快捷栏
│   │   ├── TabBar.vue               # 工具页多标签栏
│   │   └── CommandPalette.vue       # 全局命令面板
│   ├── composables/
│   │   ├── usePluginStore.ts        # 插件系统核心（安装/卸载/加载/路由注册）
│   │   ├── useFavorites.ts          # 收藏管理
│   │   ├── useToolOrder.ts          # 拖拽排序
│   │   ├── useTabs.ts               # 多标签管理
│   │   └── useSettings.ts           # 用户设置
│   ├── data/tools.ts                # ToolInfo 类型定义
│   ├── router/index.ts              # 路由（首页 + 商店，工具路由动态注册）
│   └── views/
│       ├── HomeView.vue             # 首页（工具卡片网格）
│       └── PluginStoreView.vue      # 插件商店
├── src-tauri/                       # Rust 后端
│   └── src/
│       ├── lib.rs                   # Tauri 命令注册
│       ├── plugin_manager.rs        # 插件管理（npm 注册表、安装、卸载、bundle 读取）
│       └── system_commands.rs       # 系统信息命令
└── vite.config.ts
```

## 🔌 插件系统架构

主应用本身不包含任何工具，所有工具以插件形式存在：

```
npm registry
    │
    ├── usefultools-plugin-official     ← 官方工具包（27 个工具）
    ├── usefultools-plugin-xxx          ← 社区插件
    └── @scope/usefultools-plugin-xxx   ← scoped 社区插件
    │
    ▼
Rust 后端（plugin_manager.rs）
    │  搜索 npm → 下载 tarball → 解析 plugin.json → 提取 .mjs bundle
    │
    ▼
前端（usePluginStore.ts）
    │  读取 bundle 源码 → ESM 重写 → new Function 执行 → 动态注册路由
    │
    ▼
用户看到工具出现在首页
```

### 插件包结构

每个插件包是一个标准 npm 包，包含：

- `plugin.json` — 工具元数据（支持单工具或多工具数组）
- `dist/*.mjs` — 构建好的 Vue 组件 bundle（CSS 已内联）

构建时 `vue` 和 `@tauri-apps/*` 作为 external，运行时由主应用通过 `window.__PLUGIN_MODULES__` 注入。

### npm 包名规则

包名（scoped 包取 `/` 后面的部分）必须以 `usefultools-plugin` 开头，且 `keywords` 包含 `"usefultools-plugin"`。

## 🧩 开发插件

推荐使用官方模板仓库快速开始：[Usefultools_Plugin_Template](https://github.com/your-username/usefultools-plugin-template)

```bash
# 1. 从模板创建仓库
# 2. 安装依赖
pnpm install
# 3. 启动 Tauri 桌面窗口开发（可调用原生 API）
pnpm tauri:dev
# 4. 构建插件 bundle
pnpm build
# 5. 发布到 npm
npm publish
```

### 调试模式

在主应用设置中开启「调试模式」，填入插件项目根目录路径（含 `plugin.json` 和 `dist/`），即可在不发布到 npm 的情况下实时加载和测试插件。

### plugin.json 示例

```json
{
  "id": "my-tool",
  "version": "1.0.0",
  "author": "Your Name",
  "icon": "build",
  "title": "我的<br/>工具",
  "subtitle": "我的工具",
  "description": "一句话描述工具功能",
  "bgColor": "bg-primary",
  "categories": ["编码开发"],
  "requires": [],
  "bundle": "dist/my-tool.mjs"
}
```

详细开发指南见模板仓库的 README。

## 📄 License

MIT
